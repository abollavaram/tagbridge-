# A real MCP session

Generated by `pnpm mcp:transcript`, which drives the stdio server in
`scripts/mcp-stdio.ts` and records what it actually returns. Nothing here
is written by hand — re-run it and it reproduces, or the docs are wrong.

The client acts as `buyer@example.com`. Two of the eight exchanges are
refusals, and they are the interesting ones: the price a client tried to
set, and the approval it tried to grant itself.

## 1. The client opens the connection.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "initialize",
  "params": {
    "protocolVersion": "2025-06-18",
    "capabilities": {},
    "clientInfo": {
      "name": "claude-desktop",
      "version": "1.0.0"
    }
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "protocolVersion": "2025-06-18",
    "capabilities": {
      "tools": {
        "listChanged": false
      }
    },
    "serverInfo": {
      "name": "tagbridge",
      "title": "TagBridge industrial connectivity",
      "version": "1.0.0"
    },
    "instructions": "TagBridge sells industrial connectivity software: OPC UA servers, protocol gateways, historian connectors and MQTT bridges.\n\nUse searchProducts when someone describes a symptom or names a protocol, vendor or part number. Use resolveCompatibility when they name what they have and what they want it to reach — it returns a deterministic bundle, not a guess. Use getPricing for any number you intend to state.\n\nYou never set a price. Send variant ids and quantities; every amount comes back from the server. A quote you draft goes to a human for approval before it can be sent, and the tool will tell you so."
  }
}
```

## 2. It asks what this server can do.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "tools/list"
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "tools": [
      {
        "name": "searchProducts",
        "description": "Search the catalogue by symptom, protocol, vendor or part number. Returns products with their smallest variant id, which is what a quote line refers to.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "What the buyer is looking for, in their words."
            },
            "limit": {
              "type": "integer",
              "minimum": 1,
              "maximum": 10
            }
          },
          "required": [
            "query"
          ],
          "additionalProperties": false
        }
      },
      {
        "name": "resolveCompatibility",
        "description": "Given a source system, a destination and a tag count, return the bundle of products that connects them, the licence tier, and any gaps. Deterministic: the rules are code, not judgement.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "sourceDevice": {
              "type": "string",
              "enum": [
                "allen-bradley",
                "siemens",
                "modicon",
                "mitsubishi",
                "bacnet",
                "dnp3-rtu",
                "iec61850-ied",
                "serial-ascii",
                "opc-da-server",
                "opc-ua-server",
                "other"
              ]
            },
            "destinationSystem": {
              "type": "string",
              "enum": [
                "sql-server",
                "postgresql",
                "influxdb",
                "snowflake",
                "mqtt-broker",
                "sparkplug-host",
                "opc-ua-client",
                "scada-hmi",
                "file"
              ]
            },
            "tagCount": {
              "type": "integer",
              "minimum": 1
            },
            "transport": {
              "type": "string",
              "enum": [
                "ethernet-ip",
                "s7comm",
                "modbus-tcp",
                "modbus-rtu",
                "profinet",
                "bacnet-ip",
                "bacnet-mstp",
                "dnp3",
                "iec-61850",
                "cc-link-ie",
                "opc-ua",
                "opc-da",
                "serial-ascii"
              ]
            },
            "redundancyRequired": {
              "type": "boolean"
            },
            "intermittentLink": {
              "type": "boolean"
            },
            "legacyFirmware": {
              "type": "boolean"
            }
          },
          "required": [
            "sourceDevice",
            "destinationSystem",
            "tagCount"
          ],
          "additionalProperties": false
        }
      },
      {
        "name": "getPricing",
        "description": "The server-computed price for a variant at a quantity, including the next volume break. This is the only source of a price; you may report what it returns and must never state a price it did not.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "variantId": {
              "type": "string"
            },
            "qty": {
              "type": "integer",
              "minimum": 1
            }
          },
          "required": [
            "variantId",
            "qty"
          ],
          "additionalProperties": false
        }
      },
      {
        "name": "createQuote",
        "description": "Draft a quote from line items. Give only variant ids and quantities — every price is computed by the server. A quote above the approval threshold enters pending_approval and cannot be sent by you.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "lines": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "object",
                "properties": {
                  "variantId": {
                    "type": "string"
                  },
                  "qty": {
                    "type": "integer",
                    "minimum": 1
                  }
                },
                "required": [
                  "variantId",
                  "qty"
                ],
                "additionalProperties": false
              }
            },
            "notes": {
              "type": "string"
            }
          },
          "required": [
            "lines"
          ],
          "additionalProperties": false
        }
      },
      {
        "name": "updateQuoteStatus",
        "description": "Move a quote to another state. Illegal transitions are refused. Approving or sending a quote requires a human with the sales or admin role — you cannot do it.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "quoteId": {
              "type": "string"
            },
            "to": {
              "type": "string",
              "enum": [
                "draft",
                "pending_approval",
                "sent",
                "viewed",
                "accepted",
                "converted",
                "expired",
                "rejected"
              ]
            },
            "reason": {
              "type": "string"
            }
          },
          "required": [
            "quoteId",
            "to"
          ],
          "additionalProperties": false
        }
      }
    ]
  }
}
```

## 3. The buyer describes what they have and what they want it to reach.

```json
{
  "jsonrpc": "2.0",
  "id": 3,
  "method": "tools/call",
  "params": {
    "name": "resolveCompatibility",
    "arguments": {
      "sourceDevice": "allen-bradley",
      "destinationSystem": "sql-server",
      "tagCount": 5000,
      "transport": "ethernet-ip"
    }
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 3,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "{\n  \"bundle\": [\n    {\n      \"role\": \"source-connectivity\",\n      \"sku\": \"TB-OPCUA-4100\",\n      \"name\": \"Meridian OPC UA Server for Allen-Bradley\",\n      \"why\": \"Reads ControlLogix, CompactLogix and MicroLogix over EtherNet/IP.\"\n    },\n    {\n      \"role\": \"destination-connector\",\n      \"sku\": \"TB-HIST-6100\",\n      \"name\": \"Streamline Connector for SQL Server\",\n      \"why\": \"Writes to SQL Server.\"\n    }\n  ],\n  \"licenseTier\": \"medium\",\n  \"gaps\": [],\n  \"rulesApplied\": [\n    \"source:allen-bradley\",\n    \"destination:sql-server\",\n    \"capacity:medium\"\n  ]\n}"
      }
    ]
  }
}
```

## 4. It turns the resolver’s SKU into something quotable.

```json
{
  "jsonrpc": "2.0",
  "id": 4,
  "method": "tools/call",
  "params": {
    "name": "searchProducts",
    "arguments": {
      "query": "TB-OPCUA-4100",
      "limit": 1
    }
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 4,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "{\n  \"hits\": [\n    {\n      \"variantId\": \"35023ae6-e836-42b2-b1c3-1493a622bd16\",\n      \"sku\": \"TB-OPCUA-4100\",\n      \"name\": \"Meridian OPC UA Server for Allen-Bradley\",\n      \"category\": \"OPC Servers\",\n      \"description\": \"<untrusted_catalog_content>\\nAn OPC UA server that reads and writes tags in ControlLogix, CompactLogix and MicroLogix controllers over EtherNet/IP, without an intermediate DDE or DA layer. Tags can be browsed straight from the controller program, imported from an L5X export, or defined by hand when the program is not available. Subscriptions are grouped per controller so a slow rack cannot stall the rest of the address space.\\n</untrusted_catalog_content>\",\n      \"reasons\": [\n        \"exact part number TB-OPCUA-4100\"\n      ]\n    }\n  ],\n  \"count\": 1\n}"
      }
    ]
  }
}
```

## 5. It checks the price. This is the only place a number comes from.

```json
{
  "jsonrpc": "2.0",
  "id": 5,
  "method": "tools/call",
  "params": {
    "name": "getPricing",
    "arguments": {
      "variantId": "35023ae6-e836-42b2-b1c3-1493a622bd16",
      "qty": 4
    }
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 5,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "{\n  \"variantId\": \"35023ae6-e836-42b2-b1c3-1493a622bd16\",\n  \"sku\": \"TB-OPCUA-4100-S\",\n  \"qty\": 4,\n  \"unitPriceCents\": 189000,\n  \"lineTotalCents\": 756000,\n  \"nextBreakAtQty\": 5\n}"
      }
    ]
  }
}
```

## 6. It tries to set the price itself. The guardrail refuses, and says why.

```json
{
  "jsonrpc": "2.0",
  "id": 6,
  "method": "tools/call",
  "params": {
    "name": "createQuote",
    "arguments": {
      "lines": [
        {
          "variantId": "35023ae6-e836-42b2-b1c3-1493a622bd16",
          "qty": 4,
          "unitPriceCents": 100
        }
      ]
    }
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 6,
  "result": {
    "isError": true,
    "content": [
      {
        "type": "text",
        "text": "Prices are computed by the server from the published volume breaks. Send variant ids and quantities only."
      }
    ]
  }
}
```

## 7. It drafts the quote properly, sending only ids and quantities.

```json
{
  "jsonrpc": "2.0",
  "id": 7,
  "method": "tools/call",
  "params": {
    "name": "createQuote",
    "arguments": {
      "lines": [
        {
          "variantId": "35023ae6-e836-42b2-b1c3-1493a622bd16",
          "qty": 4
        }
      ],
      "notes": "Allen-Bradley to SQL Server, 5,000 tags."
    }
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 7,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "{\n  \"quoteId\": \"ad8e1616-ad3e-4bf6-8e33-20c2f5090979\",\n  \"number\": \"Q-202608-001000\",\n  \"status\": \"pending_approval\",\n  \"subtotalCents\": 756000,\n  \"requiresApproval\": true,\n  \"lines\": [\n    {\n      \"variantId\": \"35023ae6-e836-42b2-b1c3-1493a622bd16\",\n      \"sku\": \"TB-OPCUA-4100-S\",\n      \"qty\": 4,\n      \"unitPriceCents\": 189000,\n      \"lineTotalCents\": 756000\n    }\n  ]\n}"
      }
    ]
  }
}
```

## 8. It tries to approve its own quote. It cannot.

```json
{
  "jsonrpc": "2.0",
  "id": 8,
  "method": "tools/call",
  "params": {
    "name": "sendQuoteEmail",
    "arguments": {
      "quoteId": "e08f409b-f6ca-4de6-9719-39d331ca32bc"
    }
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 8,
  "result": {
    "isError": true,
    "content": [
      {
        "type": "text",
        "text": "No tool named \"sendQuoteEmail\" is available to this client."
      }
    ]
  }
}
```
